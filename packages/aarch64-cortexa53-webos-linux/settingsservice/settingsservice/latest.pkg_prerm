#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop settings-service-recovery.service settings-service.service

		systemctl disable settings-service-recovery.service settings-service.service
	fi
fi
