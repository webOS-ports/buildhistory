#!/bin/sh
set -e
if systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop com.webos.service.mediacontroller.service

		systemctl disable com.webos.service.mediacontroller.service
	fi
fi
