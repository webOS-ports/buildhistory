#!/bin/sh
set -e
	update-alternatives --remove  gpsd-defaults /etc/default/gpsd.default
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop gpsd.socket gpsdctl@.service

		systemctl disable gpsd.socket gpsdctl@.service
	fi
fi
