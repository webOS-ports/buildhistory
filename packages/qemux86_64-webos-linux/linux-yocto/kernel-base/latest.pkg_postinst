set -e
	if [ ! -e "$D/lib/modules/5.15.91-yocto-standard" ]; then
		mkdir -p $D/lib/modules/5.15.91-yocto-standard
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 5.15.91-yocto-standard
	else
		depmod -a 5.15.91-yocto-standard
	fi
