set -e
	if [ ! -e "$D/lib/modules/6.6.20-yocto-standard" ]; then
		mkdir -p $D/lib/modules/6.6.20-yocto-standard
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 6.6.20-yocto-standard kernel
	else
		depmod -a 6.6.20-yocto-standard
	fi
