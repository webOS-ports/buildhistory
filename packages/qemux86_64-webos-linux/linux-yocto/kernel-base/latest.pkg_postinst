set -e
	if [ ! -e "$D/lib/modules/5.10.21-yocto-standard" ]; then
		mkdir -p $D/lib/modules/5.10.21-yocto-standard
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 5.10.21-yocto-standard
	else
		depmod -a 5.10.21-yocto-standard
	fi
