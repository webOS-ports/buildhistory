set -e
	if [ ! -e "$D/lib/modules/5.2.20-yocto-standard" ]; then
		mkdir -p $D/lib/modules/5.2.20-yocto-standard
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 5.2.20-yocto-standard
	else
		depmod -a 5.2.20-yocto-standard
	fi
