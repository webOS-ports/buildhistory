set -e
	if [ ! -e "$D/lib/modules/6.5.7-yocto-standard" ]; then
		mkdir -p $D/lib/modules/6.5.7-yocto-standard
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 6.5.7-yocto-standard kernel
	else
		depmod -a 6.5.7-yocto-standard
	fi
