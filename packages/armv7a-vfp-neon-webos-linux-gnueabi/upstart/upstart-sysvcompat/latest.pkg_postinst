#!/bin/sh
	update-alternatives --install /sbin/reboot reboot reboot.upstart 200
	update-alternatives --install /sbin/halt halt halt.upstart 200
	update-alternatives --install /sbin/poweroff poweroff poweroff.upstart 200
	update-alternatives --install /sbin/shutdown shutdown shutdown.upstart 200
	update-alternatives --install /sbin/telinit telinit telinit.upstart 200
