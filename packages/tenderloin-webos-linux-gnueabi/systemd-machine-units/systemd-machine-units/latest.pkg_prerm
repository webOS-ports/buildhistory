#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop tenderloin-ath6kl-module.service tenderloin-swap.service

		systemctl disable tenderloin-ath6kl-module.service tenderloin-swap.service
	fi
fi
