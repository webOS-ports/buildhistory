set -e
	if [ ! -e "$D/lib/modules/6.3.3-pinephone" ]; then
		mkdir -p $D/lib/modules/6.3.3-pinephone
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 6.3.3-pinephone kernel
	else
		depmod -a 6.3.3-pinephone
	fi
