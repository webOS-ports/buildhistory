set -e
	if [ ! -e "$D/lib/modules/5.5.0-pinephone" ]; then
		mkdir -p $D/lib/modules/5.5.0-pinephone
	fi
	if [ -n "$D" ]; then
		depmodwrapper -a -b $D 5.5.0-pinephone
	else
		depmod -a 5.5.0-pinephone
	fi
