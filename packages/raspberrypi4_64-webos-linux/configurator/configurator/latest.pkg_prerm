#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop configurator-activity.service configurator-db8.service

		systemctl disable configurator-activity.service configurator-db8.service
	fi
fi
