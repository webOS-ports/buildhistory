#!/bin/sh
set -e
if systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop com.webos.service.mediarecorder.service

		systemctl disable com.webos.service.mediarecorder.service
	fi
fi
