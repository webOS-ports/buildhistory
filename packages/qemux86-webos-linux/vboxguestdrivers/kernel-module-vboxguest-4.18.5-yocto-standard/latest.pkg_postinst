#!/bin/sh
set -e
if [ -z "$D" ]; then
	depmod -a 4.18.5-yocto-standard
else
	# image.bbclass will call depmodwrapper after everything is installed,
	# no need to do it here as well
	:
fi

if [ x"$D" = "x" ]; then
	modprobe vboxguest || true
fi
