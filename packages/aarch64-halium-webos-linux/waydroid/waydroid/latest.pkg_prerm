#!/bin/sh
set -e
if systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop waydroid-init.service waydroid-container.service

		systemctl disable waydroid-init.service waydroid-container.service
	fi
fi
