#!/bin/sh
set -e
if systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop com.webos.service.mediaindexer.service

		systemctl disable com.webos.service.mediaindexer.service
	fi
fi
