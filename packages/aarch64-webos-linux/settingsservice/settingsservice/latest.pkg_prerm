#!/bin/sh
set -e
if systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop settings-service.service settings-service-recovery.service

		systemctl disable settings-service.service settings-service-recovery.service
	fi
fi
