#!/bin/sh
set -e
if type systemctl >/dev/null 2>/dev/null; then
	if [ -z "$D" ]; then
		systemctl stop org.webosports.service.ipkg.service

		systemctl disable org.webosports.service.ipkg.service
	fi
fi
